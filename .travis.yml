os: linux
language: node_js
node_js:
  "stable"
sudo: required
services:
    - docker 
env:
 global:
   - DOCKER_USERNAME=manshaehsan
   - DOCKER_PASSWORD=loyelopassword! 
bofore_install:
   - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
   - docker build -t manshaehsan/knight:tCI .
   - docker ps 
   - docker push manshaehsan/knight:tCI
    
